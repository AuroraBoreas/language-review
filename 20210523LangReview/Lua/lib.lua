-- @ import mechanism is similar with JavaScript;

lib = {}

local var = require('var')
local func = require('func')
local stat = require('stat')
local cflow = require('cflow')
local loop = require('loop')

lib.var = var
lib.func = func
lib.stat = stat
lib.cflow = cflow
lib.loop = loop

return lib