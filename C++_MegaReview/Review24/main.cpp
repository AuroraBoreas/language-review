#include "algo.h"

int main()
{
    TS::otopic("permutation_heap");
    TS::algo_permutation_heap();

    TS::otopic("permutation_sort");
    TS::algo_permutation_sort();

    TS::otopic("structure_changer");
    TS::algo_structure_changer();

    TS::otopic("mover");
    TS::aglo_mover();

    TS::otopic("value_modifier");
    TS::algo_value_modifier();

    TS::otopic("set");
    TS::algo_set();

    TS::otopic("query_property");
    TS::algo_query_property();

    TS::otopic("query_value");
    TS::algo_query_value();

    TS::otopic("raw_memory");
    TS::algo_raw_memory();

    TS::otopic("secret_rune");
    TS::algo_secret_rune();

    TS::otopic("lone_island");
    TS::algo_lone_island();

    return 0;
}
